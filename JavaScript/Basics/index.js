//variable - bucket to store data in it and you can name the variable to identify
let learnCode = true;
let codeisValid = true;

/* Note : semicolon is not mandatory it is recommended so that if others are coming in place of your then he/she will understand the statement if you written code in a good way */

const alpha = 12;
const beta = 121;

// alpha = 120;const value can't change once it is assign before
console.log(alpha);

var a = 10;
var q = true;

//it is dynamically typed language there is no need to type data type in it
let type = typeof (a);
let type2 = typeof (q);

console.log(type);
console.log(typeof(alpha));
console.log(typeof(learnCode));
console.log(type2);